<?xml version="1.0" encoding="UTF-8"?>

<systemdatarecorder>

  <logs>
    <!-- Default Path -->
    <base path="/opt/sdr/log" description="base log directory" />
    <current path="/opt/sdr/log/current" description="current log directory" />
    <daily path="/opt/sdr/log/daily" description="daily log directory" />
  </logs>

  <!-- Host Id -->
  <hostid></hostid>

  <!-- Transport Analytic Settings -->
  <transport description="Transport settings">

    <datafile>
      <sdrd name="sys"  description="system data, sysrec.sdrd" />
      <sdrd name="cpu"  description="per-cpu data, cpurec.sdrd"  />
      <sdrd name="disk" description="per-disk data, diskrec.sdrd" />
      <sdrd name="nic"  description="per-NIC data, nicrec.sdrd"  />
      <sdrd name="hdw"  description="inventory data, hdwrec.sdrd"  />
    </datafile>

    <!-- appliance 1 -->
    <appliance name="188.127.209.196" ver="1.0.0" description="">

      <sid>2ff9851ecfa8f07d39b367a70c1c4f34</sid>

      <datafeed>http|https</datafeed>

      <dataport>9001|443</dataport>

      <keepalive>0</keepalive>

      <!-- via proxy -->
      <proxy>
        <proxyserver></proxyserver>
        <proxport></proxyport>
      </proxy>

    </appliance>

    <!-- appliance 2 -->
    <appliance name="88.27.09.1" ver="1.0.0" description="">

      <sid>00f9851ecfa8f07d39b367a70c1c4f34</sid>

      <datafeed>http|https</datafeed>

      <dataport>9001|443</dataport>

      <keepalive>0</keepalive>

      <!-- via proxy -->
      <proxy>
        <proxyserver></proxyserver>
        <proxport></proxyport>
      </proxy>

    </appliance>

  </transport>

</systemdatarecorder>
